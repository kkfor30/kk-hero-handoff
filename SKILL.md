---
name: kk-hero-handoff
description: "为已有网页首屏准备入场视频素材要求，接入或修复视频居中播放、回位及静态 Hero 衔接；支持已有视频和外部生成后回传。"
---

# Hero 入场动画交接

帮助用户把已确定的静态首屏做成“视频居中播放 → 场景回位 → 静态首屏接管”的效果。优先保留现有布局、文案和已认可素材。无需其他 Skill、生成平台账号或项目历史。

## 从用户已有条件开始

- 只有静态首屏：读取 [素材准备](references/material-preparation.md)，识别最终场景、整理动作想法及首尾帧/视频要求，交付可直接使用的提示词和文件清单。
- 已有视频：检查场景与视频，再接入。已有明确认可的内容无需重复询问。
- 已经接入但不满意：定位是视频表演、画面匹配、网页位置还是时序问题，优先修复对应部分。
- 只要求分析：只读检查并交付结论，不改项目。
- 没有静态页面：说明需要先确定首屏和落位目标；可以先整理素材要求，不自行重设计整个网站。

涉及图片或视频生成时，按用户选择的工具或平台另行执行。本 Skill 不包含生成 API；缺少工具时交付提示词和素材要求，准确说明哪些图仍未生成。保持图像确认、视频确认和网页体验确认的区别。

## 接入步骤

1. 读取项目约定，确定目标场景、静态原图、视频、需恢复的 HTML 元素及既有控制器。截图只作布局参考，不能默认当成不含文字的场景底图。
2. 读取 [素材合同](references/asset-contract.md)，标记匹配、待确认、可修复差异或缺失。发现问题时说明具体差异及最短修复路径，避免停在“请提供合格视频”。
3. 使用 Python 3.11+、FFmpeg/FFprobe 执行媒体检查。缺少依赖先报告具体缺项，按环境提供安装步骤；不可静默声称检查通过。
4. 读取 [接管画面](references/handoff-frame.md)，选择实际帧接管或高清图接管。抽取并记录接管帧、时间和视频哈希；通过脚本将记录与视频版本核对。
5. 读取 [接入与参数](references/integration.md) 和 [时序](references/handoff-state-machine.md)。简单场景可沿用同一容器的变换；需要避开祖先裁剪时使用独立视频层。模板只实现后者，不能宣称已支持所有 CSS 结构。
6. 以真实播放时间协调回位、文字显示、尾帧冻结及淡出；等待静态图解码。沿用已认可的片段，不因网页坐标问题重新生成视频。
7. 完成 [验证](references/qa-checklist.md)，技术结果与用户视觉验收分别报告。

## 工具和模板

所有路径相对于本 Skill。在项目已有素材目录工作，不要求用户迁移目录。

    python -X utf8 scripts/probe_intro.py <静态图> <视频>
    python -X utf8 scripts/extract_tail.py <视频> <新接管帧.png>
    python -X utf8 scripts/validate_handoff.py --static <静态图> --video <视频> --report <新接管帧.json>

extract_tail 默认提取最后一个实际视频帧，可用 --frame 或 --at 选择更早的稳定画面。输出已存在时拒绝覆盖。
validate_handoff 只证明规格和版本关系，不能证明视觉无缝。

templates/hero-intro.js 是 ES module，必须通过 createHeroIntro(root, options).start() 显式启动；不会自动扫描启动。
templates/integration.example.html 展示加载位置、首屏防闪和失败超时恢复。先适配项目框架再接入，不覆盖已有全局页面样式。

## 交付

向用户说明当前完成到哪一步、具体文件、还缺什么及下一步动作；技术状态用中文解释，避免只给陌生状态码。
只有用户确认真实页面后才报告视觉验收通过。素材与运行时代码只留在用户项目，Skill 中不保留私人素材、密钥或机器路径。
